import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

const FirstScreen = ({ navigation }) => {
    return (
     
       <View style={{flex:1,backgroundColor:'#00ff00',top:60}}>
           <Text>Home Screen xxx</Text>
         </View>

    );
  };

export default FirstScreen;
